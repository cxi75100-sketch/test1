# TASK-035 Zcode 整改独立复核

复核时间：2026-09-11 20:14 +08:00
交付报告：`knowledge/zcode_fix_report_2026-09-11.md`
结论：**部分通过，暂不建议提交。** 核心差异模型与现有测试通过，但新增修改明细在真实规模下会导致导入弹窗布局溢出。

## Findings

### P1 批量详情变化会使导入预览弹窗溢出

证据：

- `lib/features/import/widgets/import_preview_dialog.dart:58-71` 把 `_DiffSummary` 放在不可滚动区域，只有下方课程列表使用 `Flexible + ListView`。
- `lib/features/import/widgets/import_preview_dialog.dart:158-165` 用展开运算把全部 `changed` 项一次性渲染为普通 Column 子节点；每门课程又会生成课程标题和全部字段变化行。
- 复核时构造 27 门课程、每门仅教师发生变化，在 360×800 逻辑像素视口运行 widget test，稳定得到：`A RenderFlex overflowed by 698 pixels on the bottom.`
- `test/import_preview_dialog_test.dart:64-139` 现有测试最多只放 1 条修改，没有覆盖批量变化或断言布局无异常。
- Zcode 交付报告 `knowledge/zcode_fix_report_2026-09-11.md:132-136` 已识别“弹窗理论上可能超高”，但把它留作可选项；复核证明它不是理论风险，而是可复现缺陷。

影响：新学期、教师/教室批量调整或解析规则变化时，多门同 id 课程可能同时进入 `changed`。用户会遇到黄黑溢出条或无法完整查看/操作导入弹窗，因此任务书“清楚显示修改项”和“UI 测试真实覆盖”尚未完全达成。

整改要求：

1. 给差异明细设置有界高度并提供滚动，或把弹窗内容重构为单一、受约束的滚动区域；不得简单截断而隐藏变化。
2. 保证确认/取消按钮始终可操作，课程列表与差异明细均能查看。
3. 新增回归测试：至少 27 条 `changed`、每条 1 个字段变化，在 360×800 逻辑像素视口 `tester.takeException()` 为 null。
4. 回归测试还应滚动到最后一条修改，确认内容确实可达，而不是靠裁剪消除 overflow。

### P3 小组件数据流文档仍引用已删除的接口名

`knowledge/home_widget.md:32` 仍写 `firstSemester`，实际接口早已更名为 `currentSemester()`。建议同步为当前接口，避免下一位 Agent 按不存在的方法理解数据流。

### P3 交付报告的知识库文件计数不一致

`knowledge/zcode_fix_report_2026-09-11.md:197` 称“8 文件同步”，但报告第 2.3 节列出 9 个被修改的既有知识库文件（不含新增交付报告）。这不影响代码，但应修正交付证据口径。

## 已确认通过

- `diffImportedCourses` 的 previous/next 双侧均过滤 `CourseSource.ncpu`。
- `ImportDiff.changed` 保留新旧课程，并按值比较任务书要求的字段。
- `isEmpty` 同时考虑 added、removed、changed。
- 单条修改的 UI 会显示三类计数、旧值到新值及空值占位符。
- `flutter analyze`：独立重跑通过。
- `flutter test`：独立重跑 111/111 通过。
- `gradlew :app:testDebugUnitTest --rerun-tasks`：独立强制重跑，BUILD SUCCESSFUL。
- 临时溢出复核测试已在取得失败证据后删除，未留下额外测试文件。

## 验收状态

- TASK-035：`PARTIAL`，核心逻辑通过，UI 批量变化场景未通过。
- Git 邮箱：仍等待用户提供目标邮箱；未改配置、未重写历史，符合任务书边界。
- 真机：本轮无设备，未新增真机结论。
- 提交/推送：尚未执行；应先修复 P1 并重跑验证。

## 后续处置

`CONFIRMED`（2026-09-11 20:22 +08:00，TASK-037）：上述 P1 已修复。导入弹窗改为高度不超过视口 65% 的单一滚动区，差异明细、课程列表与底部提示均可到达，确认/取消按钮固定在滚动区外。新增 27 条修改、360×800 视口回归测试，确认无布局异常、可滚到最后一条修改及课程列表底部；全量 `flutter test` 112/112 通过。两个 P3 文档问题也已修正。
