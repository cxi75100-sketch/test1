# Agent 工作规范

本项目维护可供不同 Agent 接手的本地知识库。任何 Agent 开工前必须按顺序阅读：

1. `AGENTS.md`
2. `knowledge/current_state.md`
3. `knowledge/tasks.md`
4. 与当前任务有关的专题文档

禁止仅依赖聊天上下文理解项目状态。代码、测试和知识库必须同步更新。

## 工作流程

1. 阅读知识库并检查现有实现。
2. 将当前任务登记到 `knowledge/tasks.md` 的 Now。
3. 查询现有接口和约定后再修改，不盲猜教务系统行为。
4. 运行最小充分验证。
5. 更新任务、当前状态、测试、变更日志及受影响的专题文档。

## 事实标记

- `CONFIRMED`：已由代码、测试、真机或真实系统验证。
- `INFERRED`：基于证据推断，但没有直接验证。
- `UNVERIFIED`：尚未验证。
- `BLOCKED`：缺少外部条件，当前无法确认。

## 安全边界

- 不保存或上传教务密码。
- 不记录学号、真实姓名、完整 Cookie、Session ID 或完整 Token。
- 不绕过验证码、认证机制，不高频抓取，只处理当前登录用户自己的课表。
- Release 模式不得输出登录会话信息。

## 编码纪律

Codex八荣八耻：
以瞎猜接口为耻，以认真查询为荣；
以模糊执行为耻，以寻求确认为荣；
以臆想业务为耻，以人类确认为荣；
以创造接口为耻，以复用现有为荣；
以跳过验证为耻，以主动测试为荣；
以破坏架构为耻，以遵循规范为荣；
以假装理解为耻，以诚实无知为荣；
以盲目修改为耻，以谨慎重构为荣。
